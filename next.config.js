/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['store.storeimages.cdn-apple.com', 'example.com'],
      },
      reactStrictMode: false,
}

module.exports = nextConfig
